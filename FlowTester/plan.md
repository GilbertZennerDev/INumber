The plan is to build a tester which documents its flow across a program

the first approach is to check the first executed function in a program, then check all the following steps until the end of the main function is reached